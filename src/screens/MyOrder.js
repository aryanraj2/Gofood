import React from 'react'

// MyOrder removed — orders are no longer supported by the backend.
// This placeholder prevents import errors; it's safe to delete the file.

export default function MyOrder() {
  return null
}